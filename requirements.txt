# 后端运行依赖
fastapi>=0.115
uvicorn[standard]>=0.30
websockets>=12
mss>=9.0
Pillow>=10.0
pyautogui>=0.9.54
pyperclip>=1.8
# pyatspi 是 Linux 发行版包，不发布到 PyPI/conda-forge，不能用 pip 安装。
# 需要 find_element 时请安装系统包：
#   Ubuntu/Debian: sudo apt install python3-pyatspi
#   Fedora:        sudo dnf install python3-pyatspi
openai>=1.40
pydantic>=2.7
tomli>=2.0
python-multipart>=0.0.9
PyYAML>=6.0
requests>=2.31
rich>=13.0
mcp>=2.1.1

# 测试依赖
pytest>=8.0
httpx>=0.27
